server:
  port: 6900
eureka:
  instance:
    hostname: peer2
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://peer1:7900/eureka/,http://peer3:5900/eureka/
